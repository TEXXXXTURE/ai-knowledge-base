# 更新路径 · ROADMAP

> 这份文件只回答一个问题：**接下来做什么，按什么顺序做。**
> 待完成的事按优先级排在前面，已经立住的部分放在末尾当基线。

**目录**

1. [怎么读这份路线图](#how)
2. [M1 · 先把改动落盘](#m1)
3. [M2 · 把六套分类收敛成一套](#m2)
4. [M3 · 让两张关系图对上账](#m3)
5. [M4 · 打通同步与投稿闭环](#m4)
6. [M5 · 补齐内容与搜索](#m5)
7. [基线：已经立住的部分](#done)
8. [悬而未决的取舍](#open)

---

<a id="how"></a>

## 怎么读这份路线图

M1 → M4 是有顺序的：分类不定，数据层的字段就不敢定；关系图不对账，图查询就没有意义。M5 可以随时穿插做，不卡任何人。

每项都标了状态：

| 状态 | 含义 |
|---|---|
| **待决策** | 需要人拍板，不该由执行方替你定 |
| **可开工** | 没有前置依赖，随时能做 |
| **被阻塞** | 等前面某项完成 |

---

<a id="m1"></a>

## M1 · 先把改动落盘 `可开工`

**现状**：仓库只有 1 个历史提交（`f8cece5`），本轮全部工作尚未提交 —— 前端重置、断链修复、Schema 校验器、tool-intel 并入、设计页，全在本地。

**要做的**：分成语义清晰的提交落盘，而不是压成一坨。建议拆成五个：

1. `[前端] 重置为 Astro + Pagefind，替换 MkDocs`
2. `[数据] 修复 5 处断边与 1 组重名`
3. `[校验] 新增 validate_content.py 并接入 CI`
4. `[并入] tool-intel-wiki 内容并入（工具库 / 情报日报）`
5. `[文档] 新增设计页与更新路径`

**为什么排第一**：不落盘，后面所有改动都是单点。这是唯一一件「越早越好」的事。

---

<a id="m2"></a>

## M2 · 把六套分类收敛成一套 `待决策`

**现状**：同一个库里六套分类并行，且互相打架。

| 体系 | 位置 | 值数 | 实际被谁用 |
|---|---|---|---|
| 目录一级 | 文件路径 | 6 | 前端导航的**唯一**真来源 |
| 目录二级 | 文件路径 | 15 | 同一棵树 |
| `category` | frontmatter | 11 | 只当文字标签 |
| `dimension` | frontmatter | 25 | 只当文字标签 |
| `granularity` | frontmatter | 20 | 只当文字标签 |
| 首页栏目 | `docs/index.md` | 7 | 已失效，与目录对不上 |

冲突实证：`工具` 这个 category 横跨 `06-IT行业`（47 条）和 `03-表层概念`（4 条）两个目录；`dimension` 已长到 25 个值却只装 139 条 —— 平均每个值 5.5 条，分类碎到失去聚合价值。

**候选方案（三选一）**：

- **A. 一主一辅** —— 目录树为唯一等级分类，最多保留一个标签字段做筛选，其余删或并。**推荐**
- **B. 只留目录树** —— 最干净，但失去「跨领域找同类」的能力
- **C. 先不动** —— 接受现状，但必须先给它写规则，否则新值还会继续长

**连带影响**：这一项定了，数据层的分类字段才能定（现在 `dimension` 只能用文本存，正是因为值域还在长）。

**同时要做**：`DESIGN-SPEC.md` 已落后实现两代 —— 写的是 `related` 字符串数组（实际是 `relations` 对象数组）、完全没提 `dimension` 与 `granularity`、还写着「文件名 = 术语名」这条恰恰是 slug 404 的源头。收敛分类时把规范重写为 v2，**每条规则都必须有对应代码**。

---

<a id="m3"></a>

## M3 · 让两张关系图对上账 `待决策`

**现状**：互联机制实际是两张分裂的图。

| | 正文超链接图 | relations 声明图 |
|---|---|---|
| 边数 | 304 | 286 |
| 可查询 | 否（只是渲染结果） | 是（结构化） |
| 两者重合 | 230 条（76%） | |

另有 **166 条单向边**（从 B 页看不到 A 引用过它），而最想要的 `prerequisite`（先修链）只有 3 条。

**要做的**：

1. **对账检查** —— 在 `validate_content.py` 增加一致性规则：正文提到但 relations 未声明 → 警告，反向同理。
2. **反向边自动推导** —— `part_of` / `applies_to` / `prerequisite` 这类语义本身可逆，推导之后 166 条单行道立刻可见。
3. **决定 `type` 是否强制** —— 当前 132/286 条边没有类型（46%），要不要强制标注。

---

<a id="m4"></a>

## M4 · 打通同步与投稿闭环 `部分被阻塞`

**现状**：飞书 Base 已建成（词条 139 / 关系 291 / 投稿表 0），往返比对 100% 无损。但它是**只读快照** —— 本地改了不会进飞书，飞书改了也不会回来。

**要做的**：

1. **定同步方向与触发时机** `待决策` —— 单向（本地 → 飞书，飞书只当阅览 + 投稿信箱）还是双向？
2. **工具库与日报进 Base** `待决策` —— 单开一张「工具表」，还是转成标准词条？
   - 推荐**单开** —— 工具的字段形态（验证状态、推荐度、来源视频）与词条不同构，硬塞会同时稀释两边的字段。
3. **投稿流闭环** `被阻塞`（等 M2 分类定了，才能定「拟分类」字段的取值范围）—— 投稿表 → 审核 → 写回 Markdown → 过四道闸门。
4. **旧文档通道去留** `待决策` —— 本地 md → 飞书 wiki 文档那条链路还在用吗？还在用就保留为「人工阅读副本」，不再参与数据流。

---

<a id="m5"></a>

## M5 · 补齐内容与搜索 `可开工`

1. **搜索索引扩展** `可开工` —— `search-index.json` 目前只索引词条，工具库与情报日报搜不到。约十分钟的活。
2. **骨架补全** `待决策` —— 有 59 条词条是「框架先行」的骨架：无来源、缺别名、是图谱孤岛。它们与 draft 状态的词条完全重合，是设计好的占位而非缺陷。是否集中补一轮？
3. **draft 展示策略** `待决策` —— 当前前端把全部词条渲染上站，包括 69 条 draft。要不要让 `status` 真正生效（未完成的降级或不出现在导航）？
4. **文件名对齐** `可开工` —— 7 处文件名与词条名不一致（已不影响路由，但读起来别扭）。

---

<a id="done"></a>

## 基线：已经立住的部分

不是待办，是起点。

**前端** —— MkDocs → Astro + Content Collections + Pagefind。原来「丑」的根因查明：亮色模式 h1 颜色 `#E6E6E6` 打在 `#F0F1F3` 背景上，对比度 1.1:1（白底白字）。现为暖白纸底 + 墨色 + 一个印刷专色朱，左树右文三栏。

**校验** —— 新增 `scripts/validate_content.py`，按 status 分级（published 必须有源，draft 允许无源），因此 CI 第一天就全绿，不需要豁免清单。已接两道：PR 反馈 + 部署硬拦。

**数据债** —— 修复 5 条断边（根因是 `relations.target` 写的是文件名而非词条名）、1 组重名（DeepSeek 家族侧改名，3 条指向它的边一个字没动）。

**两个真 bug** ——
① Astro `entry.id` 逐段 slugify 导致路由大小写 404（`Next.js` → `nextjs`，Windows 大小写不敏感所以本地全绿，Linux 上一片 404）；
② Astro 内容层缓存不失效，改了插件后部分页面仍用旧渲染产物，表现为「同一页里正确与错误链接并存」，极易误判 —— 必须清 `.astro` 再构建。

**数据层** —— 飞书 Base 三张表建成，139 + 291 条灌入，往返逐字段比对 100% 无损。

**并入** —— tool-intel-wiki 的 1 篇工具拆解 + 1 期情报日报并入本站，成为独立内容域，并靠「关联词条」字段接回关系网（解析不到时渲染为「词条尚未建立」，不静默吞掉）。

**质量** —— 构建 164 页，26010 条站内链接 0 断链。

---

<a id="open"></a>

## 悬而未决的取舍

这些不是任务，是还没想清楚的问题。列在这里是为了别忘。

- 分类字段到底该有几个？一个都不留会不会更干净？
- 关系类型该不该强制？46% 无类型是正常形态还是欠账？
- 骨架词条的价值在「占位」还是「节点」？如果只是占位，它们该不该出现在关系网里？
- 数据层的定位是「镜像」还是「主库」？如果哪天在飞书里编辑更方便，源头会不会漂移？

---

*最近更新：2026-09-25*
